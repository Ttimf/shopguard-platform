/// Версия приложения. Синхронизируется вручную с `pubspec.yaml` (version:).
/// Runtime-доступа к pubspec во Flutter нет без доп. зависимости
/// (package_info_plus) — её намеренно не добавляем в рамках этапа.
const String appVersion = '1.0.0';
